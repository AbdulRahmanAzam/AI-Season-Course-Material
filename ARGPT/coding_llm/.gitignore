# Everything in data/ is regenerated by build_data2.py + tokenizer3.py +
# pack4.py in about fifteen seconds, and out/ holds 100 MB checkpoints.
# The curriculum in tasks.py is the source of truth - these are the build.
data/
out/
__pycache__/

# the deployed API URL and its key - never commit these
.api_key.txt
.api_url.txt
